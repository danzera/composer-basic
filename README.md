# PHP Composer Starter

1. [Install Composer](https://getcomposer.org/download/)
2. Clone this repository.
3. Install dependencies via `php composer.phar install` or `composer install` (if you install Composer globally)
